name: 'amazon_product_intelligence_dbt'
version: '1.0.0'

profile: 'amazon_product_intelligence_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:

  amazon_product_intelligence_dbt:

    staging:
      +materialized: view

    intermediate:
      +materialized: table

    marts:
      +materialized: table

    ai:
      +materialized: table
